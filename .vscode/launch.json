{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "lldb",
            "request": "launch",
            "name": "Debug executable 'isee-server'",
            "cargo": {
                "args": [
                    "build",
                    "--bin=isee-server",
                    "--package=isee-server"
                ],
                "filter": {
                    "name": "isee-server",
                    "kind": "bin"
                }
            },
            "args": [],
            "cwd": "${workspaceFolder}"
        }
    ]
}